// Runs this code if the plugin is run in Figma
if (figma.editorType === 'figma') {

    // This shows the HTML page in "ui.html".
    figma.showUI(__html__, { width: 350, height: 500, title: "CoCreate", themeColors: true });

    let nodes: Array<{}> = []
    let selection: Array<{}> = []

}